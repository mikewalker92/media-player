<UserControl x:Class="MediaPlayer.Views.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Background="Blue">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Row="0" x:Name="SongViewModel" />
        <Button Grid.Column="1" x:Name="PlayMedia">Play</Button>
        <Button Grid.Column="2" x:Name="PauseMedia">Pause</Button>
        <Button Grid.Column="3" x:Name="StopMedia">Stop</Button>
    </Grid>
</UserControl>
